# matcha-bot

基础 Python 项目骨架，包含：

- `src/matcha_bot` 基础包结构
- `main.py` 可执行入口
- `tests/` 单元测试示例

## 本地运行

```bash
PYTHONPATH=src python -m matcha_bot.main
```

## 运行测试

```bash
PYTHONPATH=src python -m unittest discover -s tests
```
